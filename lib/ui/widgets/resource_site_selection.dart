import 'package:flutter/material.dart';

class ResourceSiteSelection extends StatelessWidget {
  final Function onValueChanged;

  const ResourceSiteSelection({Key? key, required this.onValueChanged})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
